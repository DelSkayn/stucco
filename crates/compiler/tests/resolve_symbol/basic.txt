fn test(a: i32, b: i32) -> i32{
    a + mul(b,2)
}

fn mul(a: i32, b: i32) -> i32{
    a * b
}
----------
DECLARED:
    module{
        [0] = test @ 'fn %test%(a: i32, b: i32) -> i32{'
        [1] = mul @ 'fn %mul%(a: i32, b: i32) -> i32{'
        stencil{
            [2] = a @ 'fn test(%a%: i32, b: i32) -> i32{'
            [3] = b @ 'fn test(a: i32, %b%: i32) -> i32{'
            block{
            }
        }
        stencil{
            [4] = a @ 'fn mul(%a%: i32, b: i32) -> i32{'
            [5] = b @ 'fn mul(a: i32, %b%: i32) -> i32{'
            block{
            }
        }
    }
USES:
    module {
        function test {
            [0] = test @ 'fn %test%(a: i32, b: i32) -> i32{'
            [2] = a @ '%a% + mul(b,2)'
            [1] = mul @ 'a + %mul%(b,2)'
            [3] = b @ 'a + mul(%b%,2)'
        }
        function mul {
            [1] = mul @ 'fn %mul%(a: i32, b: i32) -> i32{'
            [4] = a @ '%a% * b'
            [5] = b @ 'a * %b%'
        }
    }
